# W kilku zadaniach poniżej pojawiają się się wyrażanie typu "lambda x: x+2".
# Jest to bardzo skrótowe zapisanie funkcji, której argument jest po lewej stronie
# znaku ":" (u nas x) a po prawej stronie znajduje się to, co się z tym argumentem dzieje ("x+2").
# Taka notacja skraca metodę "def". Pozwala nie nazywać funckji, która jest użyta np. do testów. 


# ITERACJA
# Zadanie 1: Zdefiniuj funkcję countUnique(lista), która zwraca liczbę elementów występujących w liście dokładnie jeden raz.
Użyj iteracji.
# Działanie: countUnique([1,2,2,3,4,4,5]) = 3

# REKURENCJA
# Zadanie 2: Zdefiniuj funkcję interleave(list1, list2), która tworzy nową listę
# poprzez naprzemienne pobieranie elementów z obu list (dopóki jedna z nich się nie wyczerpie).
# Użyj rekurencji.
# Działanie: interleave([1,2,3], ['a','b','c','d']) = [1,'a',2,'b',3,'c']
# def interleave(list1, list2):

# Zadanie 3: Zdefiniuj funkcję sumList(function, list), która zwraca sumę wartości
# otrzymanych po zastosowaniu funkcji do każdego elementu listy. Użyj rekurencji:
# Działanie: sumList(lambda x: x*x, [1,2,3]) = 14 (bo (1*1) + (2*2) + (3*3) = 14)
# def sumList(function, list):

# REKURENCJA OGONOWA
# Zadanie 4: Zdefiniuj funkcję partitionList(predicate, list, results), która zwraca
# listę elementów niespełniających podanego predykatu (predykat to dowolna funkkcja
# zwracająca wartość boole'owską).
# Użyj rekurencji ogonowej (rekurencji z dodatkowym parametrem).
# Działanie: partitionList(lambda x: x > 3, [1,2,3,4,5,6], []) = [1,2,3]
# def partitionList(function, list, results):




# DRZEWA i REKURENCJA

class Node:                  # klasa drzew z maksymalnie dwoma rozgałęzieniami
   def __init__(self, data): # każdy node/węzeł ma w sobie jakąś wartość (np. liczbę)
      self.data = data       # którą można odzyskać metodą .data
      self.left = None       # ma też lewe poddrzewo
      self.right = None      # oraz prawe poddrzewo

# przykład drzewa binarnego
root = Node(1)
root.left = Node(2)
root.right = Node(3)
root.right.left = Node(4)
root.right.right = Node(5)
root.right.right.left = Node(6)
root.right.right.right = Node(7)

class RoseNode:              # klasa rose trees
    def __init__(self,data): # każdy node/węzeł ma w sobie jakąś wartość (np. liczbę)
      self.data = data       # którą można odzyskać metodą .data
      self.subtrees = []     # oraz listę poddrzew.

# przykład rose tree
root1 = RoseNode(1)
root2 = RoseNode(2)
root1.subtrees = [root2,  RoseNode(3), RoseNode(4)]
root2.subtrees = [RoseNode(5), RoseNode(6)]

# Zadanie 5: Zdefiniuj funkcję treeDepth(tree), która dla drzewa
# binarnego (Node) zwraca jego maksymalną głębokość.
# Działanie: treeDepth(root) = 4
# def treeDepth(tree):


# Zadanie dodatkowe: Zdefiniuj funkcję countLeaves(tree), która dla drzewa typu
# RoseNode zwraca liczbę liści.
# Liść to węzeł, który nie posiada żadnych poddrzew.
# Działanie: countLeaves(root1) = 4
# def countLeaves(tree):
